{
  "name": "blindsidedgames-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "dev:host": "astro dev --host",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "astro preview",
    "preview:pages": "npm run build && wrangler pages dev ./dist",
    "check": "astro check",
    "test:ids-stripe": "node --experimental-strip-types --test tests/ids-stripe.test.mjs",
    "sync:genai-prompt": "python3 scripts/sync_genai_prompt.py",
    "check:genai-prompt": "python3 scripts/sync_genai_prompt.py --check",
    "deploy:pages": "npm run build && wrangler pages deploy ./dist --project-name blindsidedgames",
    "deploy:pages:preview": "npm run build && wrangler pages deploy ./dist --project-name blindsidedgames --branch preview",
    "gallery:images": "node scripts/build-debugcats-images.mjs",
    "gallery:build": "node scripts/build-debugcats.mjs",
    "gallery:preview": "npm run gallery:build && node scripts/preview-debugcats.mjs",
    "gallery:deploy": "npm run gallery:build && cd galleries/debugcats && wrangler pages deploy ../../dist-debugcats --project-name debugcats --branch main"
  },
  "dependencies": {
    "astro": "^5.4.1"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.5.0",
    "@cloudflare/workers-types": "^4.20260702.1",
    "sharp": "0.35.3",
    "typescript": "^5.8.2",
    "wrangler": "^4.4.0"
  }
}
